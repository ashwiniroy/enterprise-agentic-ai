import { Typography } from "@mui/material";

const Logo = () => {
  return (
    <Typography
      variant="h6"
      fontWeight={700}
      color="primary"
    >
      ReviewAI
    </Typography>
  );
};

export default Logo;